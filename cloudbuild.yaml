# Cloud Build configuration for flujo_api + Channel + Frameworks
substitutions:
  _SERVICE_NAME: 'flujo-api'
  _REGION: 'us-central1'
  _PROJECT_ID: 'project-ceae5831-a2c9-49aa-b1c'
  _CHANNEL_API_KEY: 'test-key-001'
  _GEMINI_API_KEY: ''
  _SHORT_SHA: 'latest'

options:
  substitution_option: 'ALLOW_LOOSE'

steps:
  # Build flujo_api
  - name: 'golang:1.26-bookworm'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        cd remora-flujo && go build -o cmd/flujo_api/flujo_api ./cmd/flujo_api
    id: 'build-flujo-api'
    waitFor: ['-']

  # Build Channel
  - name: 'golang:1.26-bookworm'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        cd channel && go build -o /workspace/remora-flujo/cmd/flujo_api/channel ./cmd/channel
    id: 'build-channel'
    waitFor: ['-']

  # Build framework-echo
  - name: 'golang:1.26-bookworm'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        cd framework-echo && go build -o frameworkecho ./cmd/frameworkecho
    id: 'build-echo'
    waitFor: ['-']

  # Build framework-alfa
  - name: 'golang:1.26-bookworm'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        cd framework-alfa && go build -o frameworkalfa ./cmd/frameworkalfa
    id: 'build-alfa'
    waitFor: ['-']

  # Build framework-auditor
  - name: 'golang:1.26-bookworm'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        cd framework-auditor && go build -o frameworkauditor ./cmd/frameworkauditor
    id: 'build-auditor'
    waitFor: ['-']

  # Build framework-indexa
  - name: 'golang:1.26-bookworm'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        cd framework-indexa && go build -o frameworkindexa ./cmd/frameworkindexa
    id: 'build-indexa'
    waitFor: ['-']

  # Build framework-sabio
  - name: 'golang:1.26-bookworm'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        cd framework-sabio && go build -o frameworksabio ./cmd/frameworksabio
    id: 'build-sabio'
    waitFor: ['-']

  # Build framework-foco
  - name: 'golang:1.26-bookworm'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        cd framework-foco && go build -o foco ./cmd/foco
    id: 'build-foco'
    waitFor: ['-']

  # Build framework-hosting
  - name: 'golang:1.26-bookworm'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        cd framework-hosting && go build -o frameworkhosting ./cmd/frameworkhosting
    id: 'build-hosting'
    waitFor: ['-']

  # Build vault CLI
  - name: 'golang:1.26-bookworm'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        cd channel && go build -o bin/vault ./cmd/vault
    id: 'build-vault'
    waitFor: ['-']

  # Build framework-mensajero
  - name: 'golang:1.26-bookworm'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        cd framework-mensajero && go build -o frameworkmensajero ./cmd/frameworkmensajero
    id: 'build-mensajero'
    waitFor: ['-']

  # Build framework-contactos
  - name: 'golang:1.26-bookworm'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        cd framework-contactos && go build -o frameworkcontactos ./cmd/frameworkcontactos
    id: 'build-contactos'
    waitFor: ['-']

  # Build framework-tareas
  - name: 'golang:1.26-bookworm'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        cd framework-tareas && go build -o frameworktareas ./cmd/frameworktareas
    id: 'build-tareas'
    waitFor: ['-']

  # Build Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/${_PROJECT_ID}/${_SERVICE_NAME}:${_SHORT_SHA}'
      - '-t'
      - 'gcr.io/${_PROJECT_ID}/${_SERVICE_NAME}:latest'
      - '-f'
      - 'remora-flujo/cmd/flujo_api/Dockerfile'
      - '.'
    id: 'build-docker'
    waitFor:
      - 'build-flujo-api'
      - 'build-channel'
      - 'build-echo'
      - 'build-alfa'
      - 'build-auditor'
      - 'build-indexa'
      - 'build-sabio'
      - 'build-foco'
      - 'build-hosting'
      - 'build-vault'
      - 'build-mensajero'
      - 'build-contactos'
      - 'build-tareas'

  # Push to GCR
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/${_PROJECT_ID}/${_SERVICE_NAME}:${_SHORT_SHA}'
    id: 'push-sha'
    waitFor: ['build-docker']

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/${_PROJECT_ID}/${_SERVICE_NAME}:latest'
    id: 'push-latest'
    waitFor: ['build-docker']

images:
  - 'gcr.io/${_PROJECT_ID}/${_SERVICE_NAME}:${_SHORT_SHA}'
  - 'gcr.io/${_PROJECT_ID}/${_SERVICE_NAME}:latest'
